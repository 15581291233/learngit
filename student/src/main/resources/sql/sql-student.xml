<?xml version="1.0" encoding="UTF-8"?>
<sql-definition>

    <select id="queryStudentList">
        SELECT * FROM student
        WHERE isDelete = 0
        AND name = ${name}
        AND phone = ${phone}
        AND idCard = ${idCard}
        AND DATE(create_time) = ${signDate}
    </select>
    
    <select id="queryUserByNamePwd">
        SELECT * FROM user
        WHERE username = ${username}
        AND password = ${password}
    </select>
    
    <select id="queryStudentIsExist">
        SELECT COUNT(*) FROM student
        WHERE idCard = ${idCard}
        AND isDelete = 0
    </select>
    
    <update id="updateStudentState">
        UPDATE student SET isDelete = 1
        WHERE isDelete = 0
    </update>

</sql-definition>